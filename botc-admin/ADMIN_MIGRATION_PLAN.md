# 📋 管理端云对象迁移计划

## 🎯 项目目标

将管理端（botc-admin）的 **9个云函数** 迁移到 **3个云对象**，遵循客户端已验证的云对象架构模式。

**参考成功案例**: 客户端已完成 10个云对象、77个方法的迁移

---

## 📊 迁移概览

### 当前状态
- **云函数数量**: 9 个
- **代码组织**: 分散
- **维护难度**: 中等

### 目标状态
- **云对象数量**: 3 个
- **方法数量**: 30-40 个
- **代码组织**: 模块化
- **维护难度**: 低

---

## 🏗️ 云对象架构设计

### 1️⃣ Admin 云对象（核心管理）

**职责**: 管理端核心功能

**包含模块**:
```
admin/
├── index.obj.js          # 15-20 个方法
└── package.json
```

**方法列表**:

#### 认证管理（3个方法）
1. `getCertifications(options)` - 获取认证列表
2. `approveCertification(certId)` - 审核通过
3. `rejectCertification(certId, reason)` - 拒绝认证

#### 举报管理（5个方法）
4. `getReports(options)` - 获取举报列表
5. `handleReport(reportId, handleResult, remark)` - 处理举报
6. `rejectReport(reportId, remark)` - 驳回举报
7. `getReportStats()` - 举报统计
8. `deleteReportedContent(contentType, contentId)` - 删除内容

#### 系统消息（2个方法）
9. `sendSystemMessage(userId, type, title, content, related)` - 发送消息
10. `sendBatchMessages(userIds, messageData)` - 批量发送

#### 敏感词管理（6个方法）
11. `getSensitiveWords(options)` - 获取敏感词列表
12. `addSensitiveWord(wordData)` - 添加敏感词
13. `updateSensitiveWord(wordId, wordData)` - 更新敏感词
14. `deleteSensitiveWord(wordId)` - 删除敏感词
15. `importSensitiveWords(words)` - 批量导入
16. `toggleSensitiveWord(wordId, status)` - 启用/禁用

#### 用户管理（3个方法）
17. `warnUser(userId, reason)` - 警告用户
18. `banUser(userId, reason)` - 封禁用户
19. `unbanUser(userId)` - 解封用户

**预估**: 15-20 个方法

---

### 2️⃣ AdminScript 云对象（剧本管理）

**职责**: 管理端剧本高级功能

**包含模块**:
```
admin-script/
├── index.obj.js          # 6-8 个方法
├── preview-generator.js  # 预览图生成器
└── package.json
```

**方法列表**:

#### 批量导入（2个方法）
1. `batchImportScripts(scripts)` - 批量导入剧本
2. `validateScriptData(scriptData)` - 验证剧本数据

#### 预览图生成（2个方法）
3. `generatePreview(scriptData)` - 生成预览图
4. `regeneratePreview(scriptId)` - 重新生成预览图

#### 剧本审核（3个方法）
5. `getAuditList(options)` - 获取待审核列表
6. `approveScript(scriptId)` - 审核通过
7. `rejectScript(scriptId, reason)` - 拒绝剧本

#### 剧本管理（1-2个方法）
8. `toggleScriptStatus(scriptId, status)` - 上架/下架

**预估**: 6-8 个方法

**特殊依赖**:
- `preview-generator.js` - SVG 预览图生成

---

### 3️⃣ AdminWiki 云对象（百科管理）

**职责**: Wiki 批量同步和管理

**包含模块**:
```
admin-wiki/
├── index.obj.js          # 8-10 个方法
├── parser-utils.js       # 解析工具
├── urls-config.js        # URL 配置
└── package.json          # 含 cheerio 依赖
```

**方法列表**:

#### 批量同步（2个方法）
1. `syncAll(syncType, batchSize)` - 批量同步所有
2. `syncSingle(url, forceRefresh)` - 同步单个页面

#### 角色管理（5个方法）
3. `getRoles(options)` - 获取角色列表
4. `addRole(roleData)` - 添加角色
5. `updateRole(roleId, roleData)` - 更新角色
6. `deleteRole(roleId)` - 删除角色
7. `syncRole(roleId)` - 同步角色数据

#### 日志管理（2个方法）
8. `getSyncLogs(options)` - 获取同步日志
9. `getSyncLogDetail(logId)` - 获取日志详情

#### 工具方法（1个方法）
10. `testParser(url)` - 测试解析器

**预估**: 8-10 个方法

**特殊依赖**:
- `cheerio` - HTML 解析库
- `parser-utils.js` - 解析工具函数
- `urls-config.js` - URL 配置文件

---

## 📅 开发计划

### 🔹 Phase 1: Admin 云对象（3-4天）

**Day 1-2: 核心功能**
- [x] 分析完成
- [ ] 创建 `admin` 云对象目录
- [ ] 实现 `_before` 钩子（权限验证）
- [ ] 实现认证管理（3个方法）
- [ ] 实现系统消息（2个方法）
- [ ] 单元测试

**Day 3: 举报和敏感词**
- [ ] 实现举报管理（5个方法）
- [ ] 实现敏感词管理（6个方法）
- [ ] 单元测试

**Day 4: 用户管理**
- [ ] 实现用户管理（3个方法）
- [ ] 集成测试
- [ ] 创建测试页面

**交付物**:
- ✅ `admin` 云对象（15-20个方法）
- ✅ 测试页面
- ✅ 开发文档

---

### 🔹 Phase 2: AdminScript 云对象（1-2天）

**Day 1: 批量导入和预览**
- [ ] 创建 `admin-script` 云对象目录
- [ ] 移植 `preview-generator.js`
- [ ] 实现批量导入（2个方法）
- [ ] 实现预览图生成（2个方法）

**Day 2: 审核和管理**
- [ ] 实现剧本审核（3个方法）
- [ ] 实现剧本管理（1-2个方法）
- [ ] 集成测试
- [ ] 创建测试页面

**交付物**:
- ✅ `admin-script` 云对象（6-8个方法）
- ✅ 测试页面
- ✅ 开发文档

---

### 🔹 Phase 3: AdminWiki 云对象（2-3天）

**Day 1: 同步核心**
- [ ] 创建 `admin-wiki` 云对象目录
- [ ] 配置 cheerio 依赖
- [ ] 移植解析工具
- [ ] 实现批量同步（2个方法）

**Day 2: 角色管理**
- [ ] 实现角色管理（5个方法）
- [ ] 实现日志管理（2个方法）

**Day 3: 测试和优化**
- [ ] 实现工具方法（1个方法）
- [ ] 性能优化（批处理）
- [ ] 集成测试
- [ ] 创建测试页面

**交付物**:
- ✅ `admin-wiki` 云对象（8-10个方法）
- ✅ 测试页面
- ✅ 开发文档

---

### 🔹 Phase 4: 前端适配（1-2天）

**Day 1: 页面适配**
- [ ] 分析管理端页面
- [ ] 适配认证管理页面
- [ ] 适配举报管理页面
- [ ] 适配敏感词管理页面

**Day 2: 剧本和Wiki**
- [ ] 适配剧本批量导入页面
- [ ] 适配Wiki同步页面
- [ ] 全面测试

**交付物**:
- ✅ 所有管理端页面已适配
- ✅ 功能测试通过

---

### 🔹 Phase 5: 清理和文档（0.5-1天）

- [ ] 删除旧云函数（本地）
- [ ] 删除旧云函数（云端）
- [ ] 更新文档
- [ ] 创建迁移总结
- [ ] 项目验收

**交付物**:
- ✅ 旧云函数已删除
- ✅ 完整技术文档
- ✅ 迁移总结报告

---

## 🎯 技术规范

### 1. 统一的架构模式

参照客户端云对象，使用统一的架构：

```javascript
'use strict';

// ==================== 工具函数（外部） ====================

function parseAdminId(context) {
  return context.ADMIN_UID || null;
}

function checkAdminAuth(adminId) {
  if (!adminId) {
    throw new Error('需要管理员权限');
  }
  return true;
}

function returnSuccess(data, message = '操作成功') {
  return { code: 0, message, data };
}

function returnError(code = -1, message = '操作失败', data = null) {
  return { code, message, data };
}

// ==================== 云对象主体 ====================

module.exports = {
  
  _before() {
    // 初始化数据库
    this.db = uniCloud.database();
    this.dbCmd = this.db.command;
    
    // 获取管理员ID
    this.adminId = parseAdminId(this.getClientInfo());
    
    // 验证权限
    try {
      checkAdminAuth(this.adminId);
    } catch (error) {
      throw new Error('权限验证失败: ' + error.message);
    }
    
    console.log('[admin] 管理员ID:', this.adminId);
  },
  
  async methodName(params) {
    try {
      // 业务逻辑
      return returnSuccess(data, message);
    } catch (error) {
      console.error('[admin] methodName 错误:', error);
      return returnError(500, error.message);
    }
  }
};
```

---

### 2. 权限验证

每个管理端方法必须验证管理员权限：

```javascript
_before() {
  this.adminId = parseAdminId(this.getClientInfo());
  checkAdminAuth(this.adminId);  // 抛出异常如果无权限
}
```

---

### 3. 操作日志

重要操作需要记录日志：

```javascript
async logOperation(action, target, details) {
  await this.db.collection('admin_operation_logs').add({
    admin_id: this.adminId,
    action: action,
    target: target,
    details: details,
    created_at: Date.now()
  });
}
```

---

### 4. 错误处理

统一的错误处理机制：

```javascript
try {
  // 业务逻辑
} catch (error) {
  console.error('[admin] 错误:', error);
  
  // 记录错误日志
  await this.logOperation('error', 'methodName', {
    error: error.message,
    stack: error.stack
  });
  
  return returnError(500, error.message);
}
```

---

## 📊 风险评估

### 高风险项

| 风险项 | 影响 | 概率 | 应对措施 |
|--------|------|------|---------|
| Wiki 批量同步超时 | 高 | 中 | 分批处理、增加超时时间 |
| cheerio 依赖问题 | 高 | 低 | 复用客户端方案 |
| 权限验证漏洞 | 高 | 低 | 严格测试权限 |

### 中风险项

| 风险项 | 影响 | 概率 | 应对措施 |
|--------|------|------|---------|
| 预览图生成失败 | 中 | 中 | 错误处理、降级方案 |
| 批量操作性能 | 中 | 中 | 优化批处理逻辑 |

### 低风险项

| 风险项 | 影响 | 概率 | 应对措施 |
|--------|------|------|---------|
| 前端适配 | 低 | 低 | 参照客户端经验 |
| 文档不全 | 低 | 低 | 同步编写文档 |

---

## ✅ 验收标准

### 功能验收

- [ ] 所有30-40个方法正常工作
- [ ] 权限验证正常
- [ ] 错误处理完善
- [ ] 操作日志记录完整

### 性能验收

- [ ] 单个请求响应时间 < 3秒
- [ ] 批量操作不超时（< 60秒）
- [ ] 数据库查询优化

### 代码质量

- [ ] 遵循统一架构模式
- [ ] 代码注释完整
- [ ] 无明显代码重复
- [ ] 通过代码审查

### 文档验收

- [ ] API 文档完整
- [ ] 开发文档清晰
- [ ] 迁移总结完成
- [ ] 测试用例记录

---

## 📚 参考资料

### 客户端云对象（已完成）

- `botc-miniprogram/uniCloud-aliyun/cloudfunctions/`
  - `user/` - 用户云对象（14个方法）
  - `script/` - 剧本云对象（14个方法）
  - `system/` - 系统云对象（6个方法）
  - `wiki/` - 百科云对象（9个方法，含cheerio）

### 技术文档

- `PROJECT_COMPLETE_FINAL_REPORT.md` - 客户端完成报告
- `API_DOCUMENTATION.md` - API 文档
- `WIKI_COMPLETE_VERSION_FULL.md` - Wiki 完整版说明

---

## 🎊 项目里程碑

### Milestone 1: Admin 云对象完成（Day 4）
- ✅ 15-20 个方法
- ✅ 测试通过
- ✅ 文档完成

### Milestone 2: AdminScript 云对象完成（Day 6）
- ✅ 6-8 个方法
- ✅ 测试通过
- ✅ 文档完成

### Milestone 3: AdminWiki 云对象完成（Day 9）
- ✅ 8-10 个方法
- ✅ 测试通过
- ✅ 文档完成

### Milestone 4: 前端适配完成（Day 11）
- ✅ 所有管理端页面适配
- ✅ 功能测试通过

### Milestone 5: 项目完成（Day 12）
- ✅ 旧云函数清理
- ✅ 文档完成
- ✅ 项目验收

---

## 📝 下一步行动

### 立即行动（分析阶段）✅
- [x] 分析现有云函数
- [x] 功能模块划分
- [x] 架构设计
- [x] 创建迁移计划

### 待用户确认
- [ ] 确认架构方案（推荐方案A：三个独立云对象）
- [ ] 确认开发优先级
- [ ] 确认开始时间

### 开发准备
- [ ] 创建开发分支
- [ ] 搭建测试环境
- [ ] 准备测试数据

---

_计划创建时间：2025-11-04_  
_预估总工期：6-12 天_  
_云对象数量：3 个_  
_方法总数：30-40 个_  
_状态：计划完成，待开始_ ✅

